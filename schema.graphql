# type ExampleEntity @entity {
#   id: ID!
#   count: BigInt!
#   support: Boolean! # bool
#   signaler: Bytes! # address
# }

type Fund @entity {
  id: ID!
  contract: Bytes!
  donor: Bytes! # address
  amount: BigInt!
}

type Payment @entity {
  id: ID!
  contract: Bytes!
  grantee: Bytes! # address
  amount: BigInt!
}
