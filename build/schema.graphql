# type ExampleEntity @entity {
#   id: ID!
#   count: BigInt!
#   support: Boolean! # bool
#   signaler: Bytes! # address
# }

type Fund @entity {
  id: ID!
  donor: Bytes! # address
  amount: BigInt!
}

type Payment @entity {
  id: ID!
  grantee: Bytes! # address
  amount: BigInt!
}
